# 作業ガイドライン

## Kotlin

このリポジトリでの作業は、まず [docs/kotlin-coding-rules.md](docs/kotlin-coding-rules.md) を前提に進めてください。
Kotlinの書式、命名、コメント、KDoc、テストの作り方などの規約は、基本的にこのドキュメントに集約しています。

### 最終確認

ソースコードに変更を入れたら、次を実行してください。

```bash
cd <project root> && ./gradlew check
cd <project root>/app/src && ktlint -F
```

## 自然言語

* 会話と作業プランなど、ユーザーに見せたり会話したりするときは、やさしい日本語で
  書いてください。自身を賢く見せるよりも、親切であることを優先してください。
  * 常に丁寧語（ですます）を意識してください。
  * 体言止めによって文章がかたくなっていないか注意してください。
* 変数名とコメントとドキュメントは英語で書いてください。
* ユーザーに次を提示するのをやめてください。
  * 「次のステップ」
  * 「次のステップに進むための質問」
* Markdownを積極的に利用してください。
  特に`---`で文章を区切り、わかりやすくしてください。
* （）などで文を補足するのは避け、接続語を使った自然な話し方をしてください。
* 原則、断定口調を使用してください。
  「可能性がある」「できるかもしれない」「そうであるかもしれない」などは、
  それを決定することが不可能な場合にのみ使用してください。

## git

gradlew.bat の変更は無視して、話題に出さないでください。
